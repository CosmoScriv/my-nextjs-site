1. Display the world of api's and define what they are
2. Enter curl

https://openstreetmap.org
curl -X POST -d "data=[out:json];node(1);out;" "https://overpass-api.de/api/interpreter"
curl -s -X POST --data-urlencode 'data=[out:json];(nwr["amenity"="restaurant"](around:3000,40.9807,-73.6918););out center;' "https://overpass-api.de/api/interpreter" | python3 -m json.tool

- add another curl her to accept city state and convert to lat, lon

2. use curl to demonstrate
3. show how app calls and displays
4. Assignment get curl to work
5. Get JSON displayed on webpage
6. Add UI to dribe the API
7. Change to accept city and state.

